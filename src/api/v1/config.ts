const parentPrefix = "/renderscan/v1"

const feedbackPrefix = parentPrefix + "/feedback"
const userPrefix = parentPrefix + "/users"
const walletPrefix = parentPrefix + "/wallets"
const paymentsPrefix = parentPrefix + "/payments"
const imagePrefix = parentPrefix + "/images"
const marketplacePrefix = parentPrefix + "/marketplace"
const nftsPrefix = parentPrefix + "/nfts"

export {
	userPrefix,
	walletPrefix,
	paymentsPrefix,
	parentPrefix,
	imagePrefix,
	marketplacePrefix,
	feedbackPrefix,
	nftsPrefix
}